# 自動テスト基盤

## 方針

テストは次の4段階で構成します。

1. **機能検証**：公開インターフェース、教材数、必須要素を確認します。
2. **境界値・異常系**：空データ、重複、循環、極端な入力、モバイル操作を確認します。
3. **結合検証**：ゲーム結果、報酬、知識グラフ、誤答支援、音声を組み合わせて確認します。
4. **利用場面検証**：学年選択から教科、ゲーム、結果、次ステージまでの流れを確認します。

## 実行方法

全テストを実行します。

```powershell
node tests/test_e2e_runner.js
```

個別に実行する場合は次のコマンドを使用します。

```powershell
node tests/test_agents.js
node tests/test_audio_fx.js
node tests/test_curriculum_dag.js
node tests/test_games.js
node tests/test_question_banks.js
node tests/test_content_safety.js
node tests/test_curriculum_traceability.js
node tests/test_adversarial_challenger.js
```

## 検証対象

### エージェントとスキーマ

- 6役の `agent.md` が存在し、YAML フロントマターを持つこと。
- `PM_SPEC_v1`、`DESIGNER_OUTPUT_v1`、`QA_BUG_REPORT_v1`、`REPAIR_PATCH_v1`、`GRAPH_MUTATION_v1` が検証されること。
- 製品管理から設計、品質保証、修復、再検証までの順序が保たれること。

### 教材と学年対応

- 学年別漢字配当表が 80・160・200・202・193・191字で、合計1,026字になること。
- 1〜2年生は国語・算数・生活だけ、3〜6年生は国語・算数・理科・社会・外国語／英語だけを表示すること。
- 算数の各学年が異なる単元構成を持ち、3年生以上が一律にてんびん問題へ置き換わらないこと。
- 部首問題の対象漢字が当該学年の配当漢字に含まれること。
- 英語の各難易度が200問以上を持ち、毎回10問を重複なしで抽出すること。

### 内容安全性

- 問題文に正解文字列や同義の答えを含めないこと。
- 社会問題の問題文や選択肢に、正解を推測できる絵文字を含めないこと。
- 選択肢が重複せず、正解が一つだけ存在すること。

### 操作と結果

- 低学年の主操作領域は56px以上、その他も十分な大きさを確保すること。
- 選択中の項目が色と枠線で強調されること。
- 失敗、時間切れ、0点ではクリア扱いにしないこと。
- クリア後に「次のステージへ」操作が表示されること。
- ホーム画面に下部ゲームボタンやエージェント観測表示が存在しないこと。

### 実ブラウザー確認

- デスクトップとモバイル相当の表示幅で、学年メニュー、教科カード、テーマ選択、ゲームモーダルを確認します。
- 英検3級・2級、短文・長文読解の文字色と背景のコントラストを確認します。
- コンソールエラー、未処理 Promise、WebGL コンテキスト喪失、操作不能な重なりを確認します。

## 出力

一括ランナーは TAP 形式の結果と `tests/test_results.json` を出力します。公開前はテスト結果と `git status` を確認し、対象変更だけをコミットします。
