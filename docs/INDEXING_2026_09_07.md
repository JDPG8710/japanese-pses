# 2026-09-07 索引修复与正式发布

发现：正式环境 /world.html 返回308到 /world，但 /world 的 canonical 和 sitemap 仍指向 /world.html；grades、privacy、terms采用了相同错误约定。Cloudflare Pages默认移除HTML扩展名。

修复：页面 canonical、og:url、站点地图和静态HTML导航统一为实际200网址；保留平台原有永久跳转和查询参数。robots.txt当前允许全站抓取，无需编造或移除不存在的禁令；robots.txt与sitemap.xml显式设置5分钟缓存并要求重新验证。参数化学习壳 /learn 保留 noindex,follow。

发布：Cloudflare Pages manabi-pop / main，https://87e3d7c4.manabi-pop.pages.dev，正式域名 https://piko-game.com 。构建资源版本7893fa43f659。此前生产部署ID：2a5077a6-fd2e-4694-8239-02c54245a04d。

验证：
- SEO源文件测试：8页、多语言hreflang、canonical、站点地图、robots和静态导航通过。
- 主回归：207项通过；隐私同意测试通过；构建成功。
- scripts/verify-indexing.mjs：8个站点地图页面各以桌面和移动Googlebot UA请求，全部200、自引用canonical、可抓取。旧HTML链接永久跳转并保留参数；www robots永久跳转到主域名；/learn继续noindex。
- scripts/verify-published.mjs：线上静态模块与构建一致、API健康和匿名写入拒绝检查通过。
- MiniGameSystem、CurriculumModeBank、EconomySystem、地图CSS、WorldPlay、WorldText及两种新背景素材均与线上字节一致，包含上一轮四项游戏修复。

限制：没有Search Console示例网址及具体抓取时间，无法断言历史robots报告的来源或已经消失。UA检查不是从Google实际网络发起的抓取，也不是收录确认。未代用户提交Search Console验证；需Google重新抓取后更新索引报告。必要的旧网址与www重定向会继续存在。

参考：
- https://developers.cloudflare.com/pages/configuration/serving-pages/
- https://developers.google.com/search/docs/crawling-indexing/301-redirects
